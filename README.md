# oneyear-fronte

Static Vue 3 frontend for OneYear forum.

## Run

```bash
npm install
npm run dev
```

Then open http://127.0.0.1:5173

Default API base URL in `main.js`:

- `http://127.0.0.1:3000/api`
